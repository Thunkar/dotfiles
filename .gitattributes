# Keep this machine's monitor layout on `git merge upstream`: `merge=ours`
# tells git to keep OUR version of the file in any merge.
#
# The driver itself is per-clone config (apply.sh registers it; on a fresh
# clone before the first apply run:  git config merge.ours.driver true).
hypr/conf/monitors.lua merge=ours
